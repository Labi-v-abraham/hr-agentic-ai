Extract leave request details from the following query.
Today's date context: use it to resolve relative dates like "tomorrow", "next Monday", etc.

IMPORTANT: start_date and end_date MUST be in YYYY-MM-DD format.
If the user does not clearly specify dates, set them to null — do NOT guess.

Query: "{{ query }}"
