Extract the task name and candidate name from this HR request.

Request: "{{ query }}"

Respond in EXACTLY this format, two lines, nothing else:
TASK: <task name>
CANDIDATE: <candidate name>

If either cannot be determined, write "UNKNOWN" for that line.
